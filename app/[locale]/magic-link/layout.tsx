export default function LoginLayout({
  children
}: {
  children: React.ReactNode
}) {
  return (
    <div>
      {/* Adicione qualquer conteúdo específico do layout de login */}
      {children}
    </div>
  )
}
