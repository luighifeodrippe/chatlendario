export default function ResetPasswordLayout({
  children
}: {
  children: React.ReactNode
}) {
  return (
    <div>
      {/* Adicione qualquer conteúdo específico do layout de redefinição de senha */}
      {children}
    </div>
  )
}
